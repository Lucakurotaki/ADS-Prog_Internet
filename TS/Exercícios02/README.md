<h2>Exercícios 01</h2>

## Q01.
<p>a)Não compila, pois a expressão "let a = 10;" define estaticamente e de forma automática a variável a como tipo number, por isso não aceita a atribuição do string "2" na expressão subsequente.</p>
<p>b)Compila, pois a expressão "let b: any = 10" define a variável b como tipo any, que aceita atribuição de qualquer tipo.</p>
<p>c)Compila, pois a expressão "let c: number = 10" define a variável c como tipo number, e na expressão seguinte é atribuído um inteiro, ou seja, valor permitido.</p>

## Q02.
<p>a)3</p>
<p>b)12</p>
<p>c)NaN</p>

## Q03.
<p>a)O programa está no arquivo "laco_enum.ts"</p>
<p>b)O programa está no arquivo "laco_enum_ind.ts"</br>
O programa imprimiu: "0 1 2 PI CE MA". Ou seja, primeiro os índices do enum e, em seguida, o conteúdo do enum.</p>

## Q04.
<p>O programa está no arquivo "diassemana.ts"</p>

## Q05.
<p>O programa está no arquivo "exibir.ts"</p>

## Q06.
<p>O programa está no arquivo "arrow_ola.ts"</p>

## Q07.
<p>O programa está no arquivo "par_arrow.ts"</p>

## Q08.
<p>O programa está no arquivo "meu_numero.ts"</p>

## Q09.
<p>O programa está no arquivo "transacao.ts"</p>